version: 2.0
jobs:
  build:
    docker:
      - image: paperist/alpine-texlive-ja
    steps:
      - checkout
      - run:
          name: Compile & Make pdf
          command: |
            cd C92 &&
            uplatex content.tex &&
            dvipdfm content.dvi &&
            cd ..

      - deploy:
          name: Deploy nodels
          command: |
            echo "＿人人人人＿" &&
            echo "＞　 更新　＜" &&
            echo "￣Y^Y^Y^Y￣" &&
            echo "" &&
            ./tools/auto-commit.sh

